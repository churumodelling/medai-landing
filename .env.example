# Переменные окружения (скопируйте в .env и заполните).
# Файл .env не попадает в репозиторий.

# Чат-бот (Vercel → Settings → Environment Variables):
# MINIMAX_API_KEY=your_key_here   # https://platform.minimax.io
